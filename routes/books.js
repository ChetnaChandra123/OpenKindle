// 🧠 placeholder file — we’ll add book CRUD routes in Step 3
const express = require('express');
const router = express.Router();

router.get('/', (req, res) => res.send('Books route placeholder'));
module.exports = router;
